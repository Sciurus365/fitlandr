% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landscape_clustering.R
\name{evaluate_landscape_clusters}
\alias{evaluate_landscape_clusters}
\title{Evaluate candidate numbers of landscape clusters}
\usage{
evaluate_landscape_clusters(
  landscapes,
  k_values = NULL,
  method = c("kmeans"),
  nstart = 25L,
  iter.max = 100L,
  seed = NULL
)
}
\arguments{
\item{landscapes}{A non-empty list of 1D or 2D landscape objects. All
landscapes must have the same dimension and grid.}

\item{k_values}{Integer candidate numbers of clusters. \code{NULL} defaults to 1
through the smaller of 10, the number of distinct landscapes, and one less
than the number of landscapes. The one-landscape case evaluates only 1.}

\item{method}{Clustering method. Currently only \code{"kmeans"} is available.}

\item{nstart}{Number of random K-means initializations.}

\item{iter.max}{Maximum number of K-means iterations.}

\item{seed}{Optional random seed. The caller's random-number state is
restored afterward.}
}
\value{
A \code{landscape_cluster_evaluation} object containing the candidate
metrics and fitted K-means models.
}
\description{
Runs K-means clustering over several candidate values of \code{k}. Landscapes
are represented by their flattened steady-state distributions, rather than
their negative-log potential values. Use \code{\link[=autoplot]{autoplot()}} on the result to draw
an elbow plot of within-cluster variance against \code{k}.
}
