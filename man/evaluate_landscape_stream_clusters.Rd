% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stream_clustering.R
\name{evaluate_landscape_stream_clusters}
\alias{evaluate_landscape_stream_clusters}
\title{Evaluate joint landscape-stream clusters}
\usage{
evaluate_landscape_stream_clusters(
  landscapes,
  streams = NULL,
  k_values = NULL,
  landscape_weight = 1,
  stream_weight = 1,
  method = c("kmeans"),
  nstart = 25L,
  iter.max = 100L,
  seed = NULL
)
}
\arguments{
\item{landscapes}{A list of two-dimensional landscapes or a
\code{group_dynamics} object.}

\item{streams}{Optional corresponding list of stream functions. It is taken
from \code{landscapes$streams} when a \code{group_dynamics} object is supplied.}

\item{k_values}{Candidate cluster counts. \code{NULL} chooses the valid range up
to 10 based on distinct joint representations and sample size.}

\item{landscape_weight, stream_weight}{Non-negative weights for the two
normalized squared-distance contributions. Both default to 1.}

\item{method}{Clustering method. Currently only \code{"kmeans"} is available.}

\item{nstart}{Number of K-means random initializations.}

\item{iter.max}{Maximum number of K-means iterations.}

\item{seed}{Optional random seed. The caller's random-number state is
restored afterward.}
}
\value{
A \code{landscape_stream_cluster_evaluation} object containing candidate
metrics, fitted models, and modality-normalization diagnostics.
}
\description{
Combines steady-state-distribution and stream-function representations for
joint K-means clustering. Each feature block is divided by the square root
of its mean pairwise squared distance before concatenation. Consequently,
with the default equal weights, landscape and stream variation make equal
average contributions to squared Euclidean distance. Stream functions are
mean-centered first to remove their arbitrary additive constants.
}
