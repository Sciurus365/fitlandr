% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stream_clustering.R
\name{evaluate_stream_clusters}
\alias{evaluate_stream_clusters}
\title{Evaluate candidate numbers of stream-function clusters}
\usage{
evaluate_stream_clusters(
  streams,
  k_values = NULL,
  method = c("kmeans"),
  nstart = 25L,
  iter.max = 100L,
  seed = NULL
)
}
\arguments{
\item{streams}{A non-empty list of \verb{2d_stream} objects, or a
\code{group_dynamics} object. All streams must use the same grid.}

\item{k_values}{Integer candidate cluster counts. \code{NULL} chooses the valid
range up to 10 based on distinct streams and sample size.}

\item{method}{Clustering method. Currently only \code{"kmeans"} is available.}

\item{nstart}{Number of K-means random initializations.}

\item{iter.max}{Maximum number of K-means iterations.}

\item{seed}{Optional random seed. The caller's random-number state is
restored afterward.}
}
\value{
A \code{stream_cluster_evaluation} object containing candidate metrics
and fitted K-means models.
}
\description{
Runs K-means over several candidate cluster counts. Each stream function is
represented by its common-grid \code{A} values after subtracting its grid mean,
because a stream function is identifiable only up to an additive constant.
}
