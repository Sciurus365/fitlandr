% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/group_dynamics.R
\name{fit_individual_dynamics}
\alias{fit_individual_dynamics}
\title{Fit vector field, landscape, and stream dynamics for one dataset}
\usage{
fit_individual_dynamics(
  data,
  x,
  y,
  lims,
  h_values = exp(seq(log(0.01), log(2), length.out = 20)),
  cv_folds = 10L,
  n_grid = 50L,
  flow_n = 20L,
  cv_args = list(),
  landscape_args = list(),
  flow_args = list()
)
}
\arguments{
\item{data}{A data frame or matrix containing one time series.}

\item{x, y}{Column names containing the two state variables.}

\item{lims}{Limits as \code{c(x_min, x_max, y_min, y_max)}. If omitted, limits
are calculated from the data and extended by 10 percent.}

\item{h_values}{Candidate bandwidths passed to \code{\link[=cv_fit_2d_vf]{cv_fit_2d_vf()}}.}

\item{cv_folds}{Number of contiguous cross-validation folds.}

\item{n_grid}{Number of common grid points per axis for vector-field
interpolation and landscape estimation.}

\item{flow_n}{Number of probability-flow vectors per axis.}

\item{cv_args}{Named list of additional arguments passed to
\code{\link[=cv_fit_2d_vf]{cv_fit_2d_vf()}}, such as \code{method}, \code{dayvar}, \code{beepvar}, or \code{na_action}.
The cross-validation default is \code{na_action = "omit_vectors"}.}

\item{landscape_args}{Named list of additional arguments passed to
\code{\link[=make_2d_ld]{make_2d_ld()}}.}

\item{flow_args}{Named list of additional arguments passed to \code{\link[=make_2d_pf]{make_2d_pf()}}.
\code{divided_by_rho} is fixed to \code{FALSE} because a stream function is fitted.}
}
\value{
An \code{individual_dynamics} object containing the input data,
cross-validation result, interpolated vector field, landscape,
probability flow, stream function, and workflow settings.
}
\description{
Runs the recommended two-dimensional fitlandr workflow for one intensive
longitudinal dataset: cross-validated vector-field estimation, common-grid
interpolation, potential-landscape estimation, probability-flow
calculation, and stream-function estimation.
}
