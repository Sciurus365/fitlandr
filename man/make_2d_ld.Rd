% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vf_ld_gen2.R
\name{make_2d_ld}
\alias{make_2d_ld}
\title{Creates a 2D Potential Landscape object from a vector field.}
\usage{
make_2d_ld(
  vf,
  linear_interp = TRUE,
  n_grid = 100,
  drift_scheme = c("upwind", "central"),
  cross_diffusion_mode = c("drop", "full"),
  boundary_mode = c("reflective", "legacy_implicit")
)
}
\arguments{
\item{vf}{A \code{vectorfield} or \code{cv_vectorfield} object representing the vector field.}

\item{linear_interp}{Logical indicating whether to use linear interpolation in predictions.}

\item{n_grid}{The number of grid points along one dimension (e.g., 100).}

\item{drift_scheme}{Drift discretization scheme. One of \code{"upwind"} or \code{"central"}.}

\item{cross_diffusion_mode}{Cross-diffusion handling mode. One of
\code{"drop"} (default, ignore \code{Dxy}) or \code{"full"} (use full diffusion tensor).}

\item{boundary_mode}{Boundary handling mode. One of \code{"reflective"} (explicit
zero-normal-flux handling) or \code{"legacy_implicit"} (index-clamping behavior).}
}
\value{
An object of class \verb{2d_static_ld} containing:
- \code{dist}: A data frame with columns x, y, d (steady-state distribution), U (potential).
- \code{plot}: A plotly surface plot of the potential landscape U, retained
for compatibility. Use \code{\link[simlandr:plotly_ld]{plotly_ld()}} to access it.
- \code{plot_2}: A ggplot2 raster plot of the potential landscape U,
retained for compatibility. Use \code{\link[ggplot2:autoplot]{autoplot()}} to access it.
- \code{vf}: The input vector field object.
- \code{ss}: The steady-state distribution matrix.
- \code{fvm_faces}: Conservative face currents from the finite-volume
solve when \code{cross_diffusion_mode = "drop"}.
- \code{fvm_compatible}: Whether those face currents remain compatible
with the returned, post-processed density.
- \code{linear_interp}, \code{drift_scheme}, \code{cross_diffusion_mode}, and
\code{boundary_mode}: Numerical settings retained for consistent
downstream probability-flow calculations.
}
\description{
Creates a 2D Potential Landscape object from a vector field.
}
