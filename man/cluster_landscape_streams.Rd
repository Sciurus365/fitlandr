% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stream_clustering.R
\name{cluster_landscape_streams}
\alias{cluster_landscape_streams}
\title{Cluster landscapes and streams jointly}
\usage{
cluster_landscape_streams(
  landscapes,
  streams = NULL,
  k,
  landscape_weight = 1,
  stream_weight = 1,
  method = c("kmeans"),
  nstart = 25L,
  iter.max = 100L,
  seed = NULL
)
}
\arguments{
\item{landscapes}{A list of two-dimensional landscapes or a
\code{group_dynamics} object.}

\item{streams}{Optional corresponding list of stream functions. It is taken
from \code{landscapes$streams} when a \code{group_dynamics} object is supplied.}

\item{k}{Selected number of clusters.}

\item{landscape_weight, stream_weight}{Non-negative weights for the two
normalized squared-distance contributions. Both default to 1.}

\item{method}{Clustering method. Currently only \code{"kmeans"} is available.}

\item{nstart}{Number of K-means random initializations.}

\item{iter.max}{Maximum number of K-means iterations.}

\item{seed}{Optional random seed. The caller's random-number state is
restored afterward.}
}
\value{
A \code{landscape_stream_clusters} object containing assignments,
landscape and stream centers, normalization diagnostics, and K-means fit.
}
\description{
Fits a fixed-K solution using the normalized joint representation described
in \code{\link[=evaluate_landscape_stream_clusters]{evaluate_landscape_stream_clusters()}}. Cluster centers are returned
separately as mean steady-state landscapes and mean-centered stream
functions on their original scales.
}
