% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_validation.R
\name{cv_fit_2d_vf}
\alias{cv_fit_2d_vf}
\title{10-Fold Cross-Validation for fit_2d_vf Bandwidth Selection}
\usage{
cv_fit_2d_vf(
  data,
  x,
  y,
  dayvar = NULL,
  beepvar = NULL,
  h_values = exp(seq(log(0.01), log(2), length.out = 20)),
  k = 10,
  n = 20,
  na_action = "omit_vectors",
  ...
)
}
\arguments{
\item{data}{A matrix/data.frame with columns x and y representing the STATE coordinates (X_t, Y_t).}

\item{x}{The name of the column representing the X coordinate.}

\item{y}{The name of the column representing the Y coordinate.}

\item{dayvar}{Optional character scalar naming the day variable. When
supplied, overnight transitions are excluded from both training and
validation.}

\item{beepvar}{Optional character scalar naming the within-day assessment
order variable. When supplied, only consecutive beeps are used in
training and validation.}

\item{h_values}{A numeric vector of candidate bandwidths to test.}

\item{k}{The number of folds for cross-validation (default is 10).}

\item{n}{Number of vector-field grid points per axis, passed to every
\code{\link[=fit_2d_vf]{fit_2d_vf()}} call.}

\item{na_action}{Missing-data handling for the final full-data fit. Fold
fits always use \code{"omit_vectors"} so vectors cannot bridge across held-out
blocks. Defaults to \code{"omit_vectors"}, which is also required for
\code{dayvar} and \code{beepvar} separators to remain effective in the final fit.}

\item{...}{Additional arguments passed to \code{\link[=fit_2d_vf]{fit_2d_vf()}} for every
fold-specific fit and the final full-data fit. These include \code{lims},
\code{vector_position}, and \code{method}.}
}
\value{
A list containing:
- 'cv_results': A data frame of 'h' values and their corresponding 'cv_mse'.
- 'h_optimal': The bandwidth that yielded the minimum 'cv_mse'.
}
\description{
Performs 10-fold cross-validation to find the optimal bandwidth 'h'
by minimizing the MSE of the predicted drift (v) against the
empirical drift of the test set (X_t+1 - X_t).
Folds are assigned as contiguous blocks to respect temporal order.
Handles single-input prediction and NA insertion.
}
