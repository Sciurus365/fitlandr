% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_comparison.R
\name{compare_minima_depths}
\alias{compare_minima_depths}
\title{Compare the potential values of two bootstrap minima}
\usage{
compare_minima_depths(object, minima, level = NULL)
}
\arguments{
\item{object}{A \code{summary_bootstrap_2d_ld} object.}

\item{minima}{Integer vector of length two giving cluster identifiers. The
reported contrast is the first minimum minus the second, \code{U[1] - U[2]}.}

\item{level}{Coverage level for the bootstrap interval. By default, uses the
level stored in \code{object$params}, or 0.95 if it is unavailable.}
}
\value{
An object of class \code{minima_depth_comparison} with components
\code{result}, a one-row data frame containing the potential contrast and its
interval, and \code{per_boot}, the paired bootstrap contrasts.
}
\description{
Computes a paired bootstrap interval for the potential difference between
two selected minima in a summarized bootstrap landscape. Pairing by
bootstrap replication makes the comparison invariant to the arbitrary
additive constant of each bootstrap potential.
}
